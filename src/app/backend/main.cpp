#include "server.h"
#include "algorithms/depthFirstSearch/dfs.h"
#include <iostream>
int main() {
    std::cout << "asdf";
    Dfs d = Dfs(5,0,1);
    d.AddConnection(0,3,1);
    // server();
    return 0;
}
